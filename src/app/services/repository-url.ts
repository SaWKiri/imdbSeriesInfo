import { environment } from '../../environments/environment';

const baseUrl = environment.apiUrl;

const repositoryUrlTVSeries = baseUrl;


export const repositoryUrl = {
  repositoryUrlTVSeries: repositoryUrlTVSeries,

};
